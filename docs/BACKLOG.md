# Backlog

`iterate-gap` skill 的唯一任务源。档内按出现顺序挑，不重排。

**格式**：`- **<slug>** — <Gap：现状 / 痛点>。**方向：** <期望动的东西>。Milestone §M<x>，Rubric §5.<y>。`

`<slug>` 是 kebab-case、一眼看出改什么。`debt-*` 前缀表示技术债。

**本文件只由 iterate-gap skill 写**：新任务通过 repopulate 产出；每轮处理完的 bullet 在当轮的 `feat(...)` commit 里删掉（决策理由写进 commit body，详见 `.claude/skills/iterate-gap/SKILL.md` §7）。手工修改请走 `docs(backlog):` 独立 commit 并说明理由。

**Repopulate 纪律（2026-04-23 后）**：每条 bullet 的 `Gap` 部分必须引用 `grep` 查到的具体 `path:line` 证据，不凭印象写"静默丢掉"/"没覆盖"等转述（连续 3 cycle 踩这坑；现在 SKILL.md §R 硬性要求）。

---

## P0（必做，阻塞当前 milestone）


## P1（强烈建议，跨 milestone debt）

- **encode-hevc-main10-sw-svt** — `grep -rn 'svt\|svthevc\|x265' CMakeLists.txt src cmake 2>/dev/null` returns empty. M10 exit criterion 3 (docs/MILESTONES.md:120) requires LGPL-clean software HEVC fallback (libx265 GPL is excluded by CLAUDE.md anti-requirements). SVT-HEVC (Apache 2.0) is the candidate per `docs/MILESTONES.md:120`. Skipped 10× consecutively as of cycle 25 — dev box lacks NASM/YASM and SVT-HEVC was retired upstream in 2021; Kvazaar (BSD-3) is an alternative if the toolchain question gets answered. **方向：** add Kvazaar OR SVT-HEVC FetchContent + `find_package`; new `src/io/svt_hevc_encoder.{hpp,cpp}` adapter producing AVPackets through libavcodec's encoder API; CMake option `ME_WITH_SVT_HEVC` (default OFF — opt-in to limit binary size); ARCHITECTURE.md whitelist update; SW path explicitly tagged non-deterministic + 1080p ceiling per VISION §3.4 LGPL ship line. Milestone §M10，Rubric §5.2。

## P2（未来，当前 milestone 不挤占）

- **lib-size-budget-hdr-codec** — `cat tools/lib_size_budget.txt` shows current `Release` budget; `grep -n 'Release_with_inference\|hevc' tools/lib_size_budget.txt` empty. M10 exit criterion 9 (docs/MILESTONES.md:126) requires bumping the budget when HEVC encode + SVT-HEVC link in. **方向：** after `encode-hevc-main10-sw-svt` lands, re-measure `Release` libmedia_engine size with `ME_WITH_SVT_HEVC=ON` vs OFF; bump `tools/lib_size_budget.txt` ceiling for the SVT path with rationale in commit body (per the file's "Bumping these numbers REQUIRES a commit body explaining" header). Milestone §M10，Rubric §5.7。
- **me-output-spec-typed-codec-enum** — `docs/PAIN_POINTS.md` 2026-04-22 records: `me_output_spec_t.video_codec` / `audio_codec` are `const char*`, every codec addition forces a new `is_xxx_spec` helper + `strcmp` branch; `video_bitrate_bps` shared without per-codec partition. With cycle 11's HEVC landing we now have 3 codecs (passthrough, h264, hevc) and the typed-option-union decision-point has fired per the bullet's threshold. **方向：** typed option union across the C ABI (`me_video_codec_t` enum + per-codec `me_h264_opts_t`, `me_hevc_opts_t`, `me_aac_opts_t` struct + `me_output_spec_t` with tagged-pointer extensions). Major ABI evolution — needs design discussion before implementation; cycle 25 deferred on §3a.10 ABI-break risk. Milestone §M7-debt (cross)，Rubric §5.2。
- **talevia-jvm-wrapper** — M7 exit criterion "在 talevia 内建 platform-impls/video-media-engine-jvm". Cross-repo task — needs talevia-side work; in this repo `bindings/jni/` is ready (cycle 28, commit 89b7275). **方向：** copy JNI wrapper to talevia tree + build.gradle.kts + replace shell-out FFmpeg in passthrough tests. This repo cycle can only prepare the JNI wrapper; integration lives in talevia. Milestone §M7，Rubric §5.5。
- **debt-bindings-kn-toolchain-port** — `bindings/kotlin-native/example/build.gradle.kts:29` pins `kotlin("multiplatform") version "2.0.20"`; `bindings/kotlin-native/CMakeLists.txt:42-49` skips the ctest on Gradle ≥ 9 (DefaultArtifactPublicationSet was removed). Bumping to Kotlin 2.1+ also breaks `bindings/kotlin-native/example/src/nativeMain/kotlin/Main.kt:33` because the K/N cinterop tool changed how opaque-handle out-params surface (`me_engine_tVar` → likely `allocPointerTo<me_engine>()` with a `CPointerVar<me_engine>` shape). On the dev box, `gradle --version` reports 9.4.1 → ctest currently skips. **方向：** port `Main.kt` to the Kotlin 2.1+ K/N cinterop convention (`allocPointerTo<me_engine>()`); bump the plugin pin to 2.1.20+; verify `gradle ... runDebugExecutableNative` succeeds (first-run downloads K/N toolchain ~500 MB, 2-5 min); drop the gradle-version gate from `bindings/kotlin-native/CMakeLists.txt`. Milestone §M7-debt (cross)，Rubric §5.5。
- **debt-cache-invalidate-asset-graph-hash** — `src/api/render.cpp:46-47` documents this: `me_cache_invalidate_asset` was originally written when DiskCache keys had shape `<asset_hash>:<src_t>` so prefix-matching the asset hash dropped derived frames; the move to graph-content-hash keys (`g:<hash>`) made that prefix match a no-op. Confirmed empirically by `examples/12_cache_invalidate`: after `invalidate_asset`, `entry_count` (AssetHashCache) drops as expected but DiskCache entries derived from the same asset still serve the next render — `miss_count` does not climb. Hosts that need real eviction call `me_cache_clear`. **方向：** maintain a side index `asset_hash → set<graph_hash>` populated on every DiskCache put and consulted by invalidate_asset (option (b) per the original bullet — O(1) per-asset eviction with a write-amplification step). Defer implementation until M11+ workloads supply DiskCache size measurements. Milestone §M6-debt (cross)，Rubric §5.4。
- **inverse-tonemap-hable-impl** — Cycle 24 landed `Linear` in `src/compose/inverse_tonemap_kernel.cpp:apply_inverse_tonemap_inplace`; `Hable` algo still returns ME_E_UNSUPPORTED at line 70 (gated by an explicit early-return). Real Hable inverse needs linear-light float buffers: the curve `f(x) = ((x*(A*x+C*B)+D*E)/(x*(A*x+B)+D*F))-E/F` is invertible only on the [0, white_scale] domain, and byte-domain inverse rounding loses enough precision that the round-trip fails VISION §3.1 byte-identity. **方向：** when a 16-bit (or RgbaF32) working buffer arrives in M11+, replace the Hable UNSUPPORTED branch with the proper linear-light inverse: read SDR byte → linearize via inverse-sRGB-EOTF → numerical inverse of Hable curve via Newton–Raphson on the [0, white_scale] domain → encode to PQ / HLG / linear-light HDR buffer. Until then leave UNSUPPORTED as the deterministic answer for Hable. Milestone §M11-followup，Rubric §5.2。
- **debt-player-rate-audio-tempo** — `src/orchestrator/player.cpp:138` (cycle 20) admits forward variable rate ∈ [0.5, 2.0] for video-only WALL-master timelines but rejects `rate ≠ 1.0` whenever `has_audio_track_` is true OR master clock is `ME_CLOCK_AUDIO` (UNSUPPORTED). Audio output runs at the timeline's native sample_rate; without time-stretching, audio would desync against scaled video. `src/audio/tempo.hpp:44` already wraps SoundTouch but `src/orchestrator/player_audio_producer.cpp` never instantiates a TempoStretcher. **方向：** plumb rate (from `clock_.rate()` snapshot or stored on Player) into `audio_producer_loop`'s per-chunk emit path; instantiate TempoStretcher with the configured rate, interleave AVFrame planes, feed to `put_samples`, pull stretched output via `receive_samples`, emit to host audio_cb. Cursor accounting: input cursor advances at sr; dispatched cursor advances at `n_smp / rate`; throttle math needs both. Tests: extend test_player audio-rate case to ME_OK + a sample-count drift bound that proves audio matches scaled video pacing. Milestone §M8-followup / cross M4，Rubric §5.5。
- **ml-inference-cmake-option** — `grep -rn 'ME_WITH_INFERENCE\|me_model_fetcher' CMakeLists.txt include src 2>/dev/null` empty. M11 exit criterion 1 (docs/MILESTONES.md:134) requires `ME_WITH_INFERENCE=ON/OFF` CMake option; OFF leaves `lib_size_budget` unchanged, ON enters `Release_with_inference` budget line. **方向：** add `option(ME_WITH_INFERENCE OFF)` to top-level CMakeLists.txt; gate inference-related FetchContent / find_package behind the option; introduce `Release_with_inference` row in `tools/lib_size_budget.txt` as a sentinel until the runtime lands. M11 seed — minimum scaffolding to start work. Milestone §M11，Rubric §5.7。
- **ml-asset-schema-types** — `grep -rn 'landmark_asset\|mask_asset\|keypoint_asset\|InferenceAsset' src/timeline 2>/dev/null` empty. M11 exit criterion 3 (docs/MILESTONES.md:136) requires JSON schema asset types: `landmark` (Nx2 floats / frame, confidence), `mask` (alpha sequence), `keypoints` (skeleton + connectivity); each carrying `model_id` / `model_version` / `quantization`. **方向：** add `me::AssetKind::Landmark / Mask / Keypoints` enum entries to `src/timeline/timeline_impl.hpp`; typed param structs in `src/timeline/timeline_ir_params.hpp`; loader_helpers dispatch for `"type": "landmark"` / `"mask"` / `"keypoints"` in `src/timeline/loader_helpers_clip_params.cpp`. Schema bump may be needed depending on whether `assetKind` is a closed enum today — verify before commit. Milestone §M11，Rubric §5.1。
- **ml-model-lazy-load-callback** — `grep -rn 'me_model_fetcher\|model_lazy_load' include/ src/api 2>/dev/null` empty. M11 exit criterion 5 (docs/MILESTONES.md:138) requires `me_model_fetcher_t` callback for host-supplied model weight loading; engine validates content_hash + license whitelist (Apache / MIT / BSD / CC-BY); rejects non-commercial / GPL / unknown. **方向：** new `include/media_engine/ml.h` (gated behind `ME_WITH_INFERENCE` so non-inference builds don't see it); `me_engine_set_model_fetcher(engine, cb, user)` wires the callback through the engine; license check happens at fetch-result-time before the runtime sees bytes. Milestone §M11，Rubric §5.2。
- **ml-effect-face-sticker-stub** — `grep -rn 'face_sticker\|FaceSticker\|landmark_asset_ref' src/effect src/compose 2>/dev/null` empty. M11 exit criterion 7+8 (docs/MILESTONES.md:140-145) requires an effect kind that consumes a landmark asset ref + ≥ 3 detection-driven effects (face_sticker, face_mosaic, body_alpha_key). **方向：** start with `face_sticker` registered-but-deferred — `EffectKind::FaceSticker` enum entry, `FaceStickerEffectParams { landmark_asset_id, sticker_uri, scale_x/y, offset_x/y }` typed struct, kernel returns ME_E_UNSUPPORTED with STUB marker pointing at this bullet's follow-up (face-sticker-impl). Lets timeline JSON authoring tools use `kind: "face_sticker"` ahead of the impl. Milestone §M11，Rubric §5.1。
- **bench-perf-budget-coverage-expand** — `grep -rn 'me::bench::run_bench\|bench_harness' bench 2>/dev/null` shows §5.7-7 perf-budget guard wired at bench/bench_perf_budget.cpp; `find bench -name 'bench_*.cpp'` lists 5 benches (compose / hdr_roundtrip / passthrough / thumbnail_png / harness). Many M10-landed paths (HEVC encode, OCIO config plumbing, parametric EQ, external clock pacer) lack matching bench coverage. **方向：** triage which kernels need bench coverage (TonemapKernel for full-frame at 4K, OcioPipeline `apply` cost vs identity, audio-effect-chain processing time per chunk); add benches under bench/ following the existing harness pattern; budget targets in tools/perf_budget.txt. M9-followup polish. Milestone §M9-followup，Rubric §5.7。
