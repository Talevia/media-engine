# Backlog

`iterate-gap` skill 的唯一任务源。档内按出现顺序挑，不重排。

**格式**：`- **<slug>** — <Gap：现状 / 痛点>。**方向：** <期望动的东西>。Milestone §M<x>，Rubric §5.<y>。`

`<slug>` 是 kebab-case、一眼看出改什么。`debt-*` 前缀表示技术债。

**本文件只由 iterate-gap skill 写**：新任务通过 repopulate 产出；每轮处理完的 bullet 在当轮的 `feat(...)` commit 里删掉（决策理由写进 commit body，详见 `.claude/skills/iterate-gap/SKILL.md` §7）。手工修改请走 `docs(backlog):` 独立 commit 并说明理由。

**Repopulate 纪律（2026-04-23 后）**：每条 bullet 的 `Gap` 部分必须引用 `grep` 查到的具体 `path:line` 证据，不凭印象写"静默丢掉"/"没覆盖"等转述（连续 3 cycle 踩这坑；现在 SKILL.md §R 硬性要求）。

---

## P0（必做，阻塞当前 milestone）


## P1（强烈建议，跨 milestone debt）

- **talevia-jvm-wrapper** — M7 exit criterion "在 talevia 内建 platform-impls/video-media-engine-jvm". Cross-repo task — needs talevia-side work; in this repo `bindings/jni/` is ready (cycle 28, commit 89b7275). **方向：** copy JNI wrapper to talevia tree + build.gradle.kts + replace shell-out FFmpeg in passthrough tests. This repo cycle can only prepare the JNI wrapper; integration lives in talevia. Milestone §M7，Rubric §5.5。
- **inverse-tonemap-hable-impl** — Cycle 24 landed `Linear` in `src/compose/inverse_tonemap_kernel.cpp:apply_inverse_tonemap_inplace`; `Hable` algo still returns ME_E_UNSUPPORTED at line 70 (gated by an explicit early-return). Real Hable inverse needs linear-light float buffers: the curve `f(x) = ((x*(A*x+C*B)+D*E)/(x*(A*x+B)+D*F))-E/F` is invertible only on the [0, white_scale] domain, and byte-domain inverse rounding loses enough precision that the round-trip fails VISION §3.1 byte-identity. **方向：** when a 16-bit (or RgbaF32) working buffer arrives in M11+, replace the Hable UNSUPPORTED branch with the proper linear-light inverse: read SDR byte → linearize via inverse-sRGB-EOTF → numerical inverse of Hable curve via Newton–Raphson on the [0, white_scale] domain → encode to PQ / HLG / linear-light HDR buffer. Until then leave UNSUPPORTED as the deterministic answer for Hable. Milestone §M11-followup，Rubric §5.2。

## P2（未来，当前 milestone 不挤占）

- **encode-hevc-main10-kvazaar-source-build** — `src/io/kvazaar_hevc_encoder.cpp:88` calls `kvz_api_get(8)` exclusively; the homebrew Kvazaar bottle's `kvazaar.h` defaults to `KVZ_BIT_DEPTH 8` and the prebuilt library doesn't expose a 10-bit `kvz_pixel` ABI. `cfg->input_bitdepth = 10` is accepted by `encoder_open` but the `picture_alloc` buffer geometry stays 8-bit. M10 exit criterion 3 (docs/MILESTONES.md:120) was satisfied by the M10 SW HEVC fallback shipping Main 8-bit only; HDR Main 10 stays on the VideoToolbox HW path. This bullet is the future-improvement to bring SW HEVC up to Main 10 for cross-host HDR. **方向：** replace `pkg_check_modules(kvazaar)` with `FetchContent_Declare(kvazaar URL ...)` + `cmake -DKVZ_BIT_DEPTH=10` so the engine links its own 10-bit-built libkvazaar.a; rewrite the encoder TU's plane-copy loops to pass uint16_t buffers. Add a fixture YUV420P 10-bit gray frame and a round-trip test that decodes the produced HEVC Main 10 and verifies pixel domain matches within ε. Note: Kvazaar's build system is autotools (no native CMake); FetchContent will need ExternalProject_Add wrapping. Milestone §M10-followup，Rubric §5.2。
- **blazeface-anchor-decode-impl** — Cycle 51 landed `me::compose::resolve_landmark_bboxes_runtime` in `src/compose/landmark_resolver.cpp:resolve_landmark_bboxes_runtime` calling `make_runtime_for_model` + `run_cached` (M11 §137/§138 production wire); the function returns ME_E_UNSUPPORTED at the decode step. `grep -rn 'sigmoid\|anchor.*decode\|bbox.*regression' src/inference src/compose` returns empty — no BlazeFace-specific decode logic exists. **方向：** new `src/compose/blazeface_decode.{hpp,cpp}` implementing BlazeFace's documented anchor decode: 896 anchors at (16×16 + 8×8) grids, sigmoid on the confidence channel, regression decode of the bbox channel into normalized [0,1]² coordinates, NMS at IoU threshold 0.3, scale to frame coordinates. Wire from resolve_landmark_bboxes_runtime's "Step 4" branch. End-to-end test exercises a real BlazeFace model via env-var-gated path (sibling to test_inference_blazeface). Milestone §M11，Rubric §5.1。
- **landmark-resolver-runtime-input-preprocess-impl** — Cycle 51's `resolve_landmark_bboxes_runtime` builds a synthetic 128×128×3 NCHW float32 input tensor (zero-filled). `grep -rn 'av_image_alloc\|sws_scale\|F32C3' src/compose/landmark_resolver.cpp` returns empty. Real BlazeFace inference needs the actual frame pixels resized + converted to planar normalized float. **方向：** add a frame preprocessing helper (e.g. `prepare_blazeface_input(rgba, w, h, stride, &tensor)`) that uses libswscale to resize to 128×128 + reorders RGBA → CHW float32 + applies BlazeFace's documented [-1, 1] normalization. Wire from `resolve_landmark_bboxes_runtime` Step 2. Milestone §M11，Rubric §5.1。
- **landmark-resolver-runtime-stage-wiring** — Cycle 51's `resolve_landmark_bboxes_runtime` exists but isn't called from production stages. `grep -rn 'resolve_landmark_bboxes_runtime' src/compose/face_sticker_stage.cpp src/compose/face_mosaic_stage.cpp` returns empty. Cycle 52 also added `resolve_mask_alpha_runtime` with the same constraint vs body_alpha_key_stage. The stage kernel runs in TaskContext which doesn't currently expose the engine pointer; runtime-mode dispatch from a kernel needs the engine to call `make_runtime_for_model`. **方向：** thread `me_engine*` through `task::TaskContext` (the natural place — kernels already receive context). Then face_sticker_stage / face_mosaic_stage / body_alpha_key_stage check the URI prefix `model:` vs file path and dispatch accordingly. Falls back to empty bboxes/mask on ME_E_UNSUPPORTED until decode bullets (`blazeface-anchor-decode-impl` / `selfie-segmentation-mask-decode-impl`) land. Milestone §M11，Rubric §5.1。
- **selfie-segmentation-mask-decode-impl** — Cycle 52 landed `me::compose::resolve_mask_alpha_runtime` calling `make_runtime_for_model + run_cached` (M11 §137/§138 production wire); the function returns ME_E_UNSUPPORTED at the decode step. `grep -rn 'sigmoid\|alpha.*mask\|mask.*upscale' src/inference src/compose/mask_resolver.cpp` returns empty — no SelfieSegmentation-specific decode logic exists. **方向：** new `src/compose/selfie_segmentation_decode.{hpp,cpp}` implementing SelfieSegmentation's documented output decode: 1×H×W float logit channel → sigmoid → uint8 quantize → bilinear upscale to frame dims. Wire from `resolve_mask_alpha_runtime` Step 4. End-to-end test exercises a real SelfieSegmentation model via env-var-gated path (sibling to test_inference_blazeface). Milestone §M11，Rubric §5.1。
- **mask-resolver-runtime-input-preprocess-impl** — Cycle 52's `resolve_mask_alpha_runtime` builds a synthetic 256×256×3 NCHW float32 input tensor (zero-filled). `grep -rn 'av_image_alloc\|sws_scale\|F32C3' src/compose/mask_resolver.cpp` returns empty. Real SelfieSegmentation inference needs the actual frame pixels resized + converted to planar normalized float. **方向：** sibling helper to `landmark-resolver-runtime-input-preprocess-impl`; one libswscale-based preprocessor that takes (rgba, w, h, stride, target_w, target_h) and produces a Tensor matching the model's expected input shape. Reuse from both runtime resolvers. Milestone §M11，Rubric §5.1。
