# Backlog

`iterate-gap` skill 的唯一任务源。档内按出现顺序挑，不重排。

**格式**：`- **<slug>** — <Gap：现状 / 痛点>。**方向：** <期望动的东西>。Milestone §M<x>，Rubric §5.<y>。`

`<slug>` 是 kebab-case、一眼看出改什么。`debt-*` 前缀表示技术债。

**本文件只由 iterate-gap skill 写**：新任务通过 repopulate 产出；每轮处理完的 bullet 在当轮的 `feat(...)` commit 里删掉（决策理由写进 commit body，详见 `.claude/skills/iterate-gap/SKILL.md` §7）。手工修改请走 `docs(backlog):` 独立 commit 并说明理由。

**Repopulate 纪律（2026-04-23 后）**：每条 bullet 的 `Gap` 部分必须引用 `grep` 查到的具体 `path:line` 证据，不凭印象写"静默丢掉"/"没覆盖"等转述（连续 3 cycle 踩这坑；现在 SKILL.md §R 硬性要求）。

---

## P0（必做，阻塞当前 milestone）


## P1（强烈建议，跨 milestone debt）

- **encode-hevc-main10-sw-svt** — `grep -rn 'svt\|svthevc\|x265\|kvazaar' CMakeLists.txt src cmake 2>/dev/null` returns empty. M10 exit criterion 3 (docs/MILESTONES.md:120) requires LGPL-clean software HEVC fallback (libx265 GPL is excluded by CLAUDE.md anti-requirements). SVT-HEVC (Apache 2.0) is the candidate per `docs/MILESTONES.md:120`. Skipped 13× consecutively through cycle 41 — dev box lacks NASM/YASM and SVT-HEVC was retired upstream in 2021; Kvazaar (BSD-3) is an alternative if the toolchain question gets answered. **方向：** add Kvazaar OR SVT-HEVC FetchContent + `find_package`; new `src/io/svt_hevc_encoder.{hpp,cpp}` adapter producing AVPackets through libavcodec's encoder API; CMake option `ME_WITH_SVT_HEVC` (default OFF — opt-in to limit binary size); ARCHITECTURE.md whitelist update; SW path explicitly tagged non-deterministic + 1080p ceiling per VISION §3.4 LGPL ship line. Milestone §M10，Rubric §5.2。
- **debt-cache-invalidate-asset-graph-hash** — `src/api/render.cpp:46-47` documents this: `me_cache_invalidate_asset` was originally written when DiskCache keys had shape `<asset_hash>:<src_t>` so prefix-matching the asset hash dropped derived frames; the move to graph-content-hash keys (`g:<hash>`) made that prefix match a no-op. Confirmed empirically by `examples/12_cache_invalidate`: after `invalidate_asset`, `entry_count` (AssetHashCache) drops as expected but DiskCache entries derived from the same asset still serve the next render — `miss_count` does not climb. Hosts that need real eviction call `me_cache_clear`. **方向：** maintain a side index `asset_hash → set<graph_hash>` populated on every DiskCache put and consulted by invalidate_asset (option (b) per the original bullet — O(1) per-asset eviction with a write-amplification step). Defer implementation until M11+ workloads supply DiskCache size measurements. Milestone §M6-debt (cross)，Rubric §5.4。
- **debt-engine-impl-compile-def-audit** — Cycle 41 caught a 7-cycles-old `mutex lock failed` flake whose root cause was `ME_HAS_SOUNDTOUCH` set as `PRIVATE` on `media_engine` (`src/CMakeLists.txt:404` pre-fix). The bug shape: `core/engine_impl.hpp` gates a struct field (`tempo_pool` `unique_ptr`) on the macro; test TUs reaching into `engine_impl.hpp` for white-box scheduler access (test_audio_chunk_graph, test_player) did not see the macro because PRIVATE compile-defs don't propagate via `target_link_libraries`, so the struct's field offsets diverged between libmedia_engine.a and the test. Symptom (random mutex bytes) was 100% confusing for 7+ cycles. The same bug shape can return — `grep -nE 'ME_HAS_(\w+).*PRIVATE' src/CMakeLists.txt` yields 4 hits today (OCIO / GPU / LIBASS / SKIA) and any of them growing a struct field in engine_impl.hpp would re-trip the trap. **方向：** new `tools/check-engine-impl-compile-defs.sh` that (a) extracts every `ME_HAS_X` reference inside `core/engine_impl.hpp`, (b) for each, checks `target_compile_definitions(media_engine ... ME_HAS_X)` is `PUBLIC` (or `INTERFACE`), (c) exits non-zero with a clear "raise PRIVATE → PUBLIC for ME_HAS_X" message otherwise. Wire into pre-commit + CI. Optional companion: a comment block at the top of `engine_impl.hpp` listing every `ME_HAS_X` it gates and the must-be-PUBLIC constraint. Milestone §M9-followup，Rubric §5.7。
- **me-output-spec-typed-codec-enum** — `docs/PAIN_POINTS.md` 2026-04-22 records: `me_output_spec_t.video_codec` / `audio_codec` are `const char*`, every codec addition forces a new `is_xxx_spec` helper + `strcmp` branch; `video_bitrate_bps` shared without per-codec partition. With cycle 11's HEVC landing we now have 3 codecs (passthrough, h264, hevc) and the typed-option-union decision-point has fired per the bullet's threshold. **方向：** typed option union across the C ABI (`me_video_codec_t` enum + per-codec `me_h264_opts_t`, `me_hevc_opts_t`, `me_aac_opts_t` struct + `me_output_spec_t` with tagged-pointer extensions). Major ABI evolution — needs design discussion before implementation; cycle 25 deferred on §3a.10 ABI-break risk. Milestone §M7-debt (cross)，Rubric §5.2。
- **talevia-jvm-wrapper** — M7 exit criterion "在 talevia 内建 platform-impls/video-media-engine-jvm". Cross-repo task — needs talevia-side work; in this repo `bindings/jni/` is ready (cycle 28, commit 89b7275). **方向：** copy JNI wrapper to talevia tree + build.gradle.kts + replace shell-out FFmpeg in passthrough tests. This repo cycle can only prepare the JNI wrapper; integration lives in talevia. Milestone §M7，Rubric §5.5。

## P2（未来，当前 milestone 不挤占）

- **lib-size-budget-hdr-codec** — `cat tools/lib_size_budget.txt` shows current `Release` budget; `grep -n 'Release_with_inference\|hevc' tools/lib_size_budget.txt` reports `Release_with_inference` rows but no HEVC-specific rows. M10 exit criterion 9 (docs/MILESTONES.md:126) requires bumping the budget when HEVC encode + SVT-HEVC link in. **方向：** after `encode-hevc-main10-sw-svt` lands, re-measure `Release` libmedia_engine size with `ME_WITH_SVT_HEVC=ON` vs OFF; bump `tools/lib_size_budget.txt` ceiling for the SVT path with rationale in commit body (per the file's "Bumping these numbers REQUIRES a commit body explaining" header). Milestone §M10，Rubric §5.7。
- **inverse-tonemap-hable-impl** — Cycle 24 landed `Linear` in `src/compose/inverse_tonemap_kernel.cpp:apply_inverse_tonemap_inplace`; `Hable` algo still returns ME_E_UNSUPPORTED at line 70 (gated by an explicit early-return). Real Hable inverse needs linear-light float buffers: the curve `f(x) = ((x*(A*x+C*B)+D*E)/(x*(A*x+B)+D*F))-E/F` is invertible only on the [0, white_scale] domain, and byte-domain inverse rounding loses enough precision that the round-trip fails VISION §3.1 byte-identity. **方向：** when a 16-bit (or RgbaF32) working buffer arrives in M11+, replace the Hable UNSUPPORTED branch with the proper linear-light inverse: read SDR byte → linearize via inverse-sRGB-EOTF → numerical inverse of Hable curve via Newton–Raphson on the [0, white_scale] domain → encode to PQ / HLG / linear-light HDR buffer. Until then leave UNSUPPORTED as the deterministic answer for Hable. Milestone §M11-followup，Rubric §5.2。
- **face-sticker-impl** — Cycle 30 landed `EffectKind::FaceSticker` + `FaceStickerEffectParams` + JSON loader dispatch + STUB-marked `apply_face_sticker_inplace` kernel that returns ME_E_UNSUPPORTED for any input (`src/compose/face_sticker_kernel.cpp:38`). M11 exit criterion 7+8 (docs/MILESTONES.md:140-145) requires the actual landmark-driven sticker overlay to ship. **方向：** the impl needs three landed prerequisites first: (1) inference runtime that consumes the Landmark asset (downstream M11 cycles, blocked on cycle 29's model-fetcher being wired to a CoreML / ONNX adapter); (2) sticker-decoder kernel for PNG / WebP via libavformat probe (mirrors `me_thumbnail_png` shape but on a pre-existing image asset); (3) compose-graph stage that overlays the sticker onto the RGBA8 frame at the resolved landmark anchor with affine scale + offset (overlaps with `RenderAffineBlit` but per-frame landmark-time-coupled). When all three land, replace the UNSUPPORTED return with: read landmark[frame_t] → compute anchor + bbox → load sticker via decoder → affine-blit at anchor. Milestone §M11，Rubric §5.1。
- **face-mosaic-impl** — Cycle 34 landed `EffectKind::FaceMosaic` + `FaceMosaicEffectParams { landmark_asset_id, block_size_px, kind: Pixelate/Blur }` + JSON loader dispatch + STUB-marked `apply_face_mosaic_inplace` kernel that returns ME_E_UNSUPPORTED for any input (`src/compose/face_mosaic_kernel.cpp:30`). The actual mosaic / blur math is deterministic byte-arithmetic; the block is the upstream wiring (same prerequisites as `face-sticker-impl`: inference runtime + landmark stream resolution + per-frame bbox computation). **方向：** when the M11 inference runtime cycles land + the landmark stream is resolvable per-frame, replace the UNSUPPORTED return with: read landmark[frame_t] → compute bbox → for Pixelate: clamp bbox to image, walk in `block_size_px` tiles, write per-channel mean × block; for Blur: same walk but with a box-filter pass at radius `block_size_px / 2`. Both are CPU-deterministic — no parallelism, no SIMD, no rand. Milestone §M11，Rubric §5.1。
- **body-alpha-key-impl** — Cycle 35 landed `EffectKind::BodyAlphaKey` + `BodyAlphaKeyEffectParams { mask_asset_id, feather_radius_px, invert }` + JSON loader dispatch + STUB-marked `apply_body_alpha_key_inplace` kernel that returns ME_E_UNSUPPORTED (`src/compose/body_alpha_key_kernel.cpp:28`). The actual mask-key math is deterministic byte-arithmetic; the block is upstream wiring (same prerequisites as `face-sticker-impl` / `face-mosaic-impl`: inference runtime + per-frame mask-asset resolution). **方向：** when M11 inference runtime cycles land + the Mask asset is resolvable per-frame as an alpha buffer, replace the UNSUPPORTED return with: read mask[frame_t] → optionally invert (`mask = 255 - mask`) → optionally box-blur the alpha edge by `feather_radius_px` → write `(mask × input.alpha) / 255` into output.alpha. CPU-deterministic. Milestone §M11，Rubric §5.1。
- **ml-inference-runtime-coreml** — `grep -rni 'CoreML\|CoreMLDelegate\|MLModel' src/ include/` returns hits only inside doc comments + ml.h's docstring (`include/media_engine/ml.h:32`); zero in actual source. M11 exit criterion 4 (docs/MILESTONES.md:135) requires CoreML inference runtime on macOS / iOS. Cycle 29 landed the model-fetcher callback API but no runtime consumes the bytes yet. **方向：** new `src/inference/coreml_runtime.{hpp,mm}` (Objective-C++ for the CoreML.framework include) + CMake `ME_WITH_INFERENCE` ON-only target; declare `me::inference::CoreMlRuntime` that takes a `me_model_blob_t` from the host fetcher, builds an `MLModel`, exposes `run(input_buffers, output_buffers)`. Apple-only; gated by `if(APPLE)` on the new sub-CMakeLists. License-wise: CoreML.framework is part of macOS / iOS (Apple's licensing of system frameworks for app developers covers this), no FetchContent. Pairs with `ml-inference-runtime-onnx` for the cross-platform fallback. Milestone §M11，Rubric §5.1。
- **ml-inference-runtime-onnx** — `grep -rn 'onnxruntime\|Ort::\|ONNXRuntime' src/ include/` returns empty. M11 exit criterion 4 (docs/MILESTONES.md:135) lists ONNX runtime as the cross-platform CPU FP32 reference path alongside CoreML. **方向：** new `src/inference/onnx_runtime.{hpp,cpp}` linked against onnxruntime (Apache 2.0 — on whitelist); FetchContent_Declare or system find_package for onnxruntime; CPU-only execution provider for determinism. Architecture: the Runtime picks CoreML on Apple HW + ONNX-CPU as the fallback / Linux path. Both implement a common `me::inference::Runtime` interface so the effect-kernel layer doesn't care which one's loaded. Milestone §M11，Rubric §5.1。
- **ml-ship-path-model-blazeface** — `grep -rni 'blazeface\|face_landmarks_v2\|selfie_seg' src/ include/` returns empty (only test fixture strings hit). M11 exit criterion 6 (docs/MILESTONES.md:139) requires "≥2 ship-path models running through the runtime + reference parity within ε". BlazeFace + portrait_segmentation are the Apache-licensed candidates per the criterion text. **方向：** when CoreML / ONNX runtimes are wired (above two bullets), add `examples/14_ml_face_landmark/` + `tests/test_inference_blazeface.cpp` that loads BlazeFace through the model-fetcher API + runs inference + asserts CoreML-vs-ONNX-CPU parity within ε. Model bytes NOT bundled (license-clean: hosts supply via fetcher); the test uses a synthetic deterministic test fixture or a host-environment-supplied blob. Milestone §M11，Rubric §5.1。
