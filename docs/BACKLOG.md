# Backlog

`iterate-gap` skill 的唯一任务源。档内按出现顺序挑，不重排。

**格式**：`- **<slug>** — <Gap：现状 / 痛点>。**方向：** <期望动的东西>。Milestone §M<x>，Rubric §5.<y>。`

`<slug>` 是 kebab-case、一眼看出改什么。`debt-*` 前缀表示技术债。

**本文件只由 iterate-gap skill 写**：新任务通过 repopulate 产出；每轮处理完的 bullet 在当轮的 `feat(...)` commit 里删掉（决策理由写进 commit body，详见 `.claude/skills/iterate-gap/SKILL.md` §7）。手工修改请走 `docs(backlog):` 独立 commit 并说明理由。

**Repopulate 纪律（2026-04-23 后）**：每条 bullet 的 `Gap` 部分必须引用 `grep` 查到的具体 `path:line` 证据，不凭印象写"静默丢掉"/"没覆盖"等转述（连续 3 cycle 踩这坑；现在 SKILL.md §R 硬性要求）。

---

## P0（必做，阻塞当前 milestone）

- **inference-asset-cache-wire-effect-stages** — `me::inference::AssetCache` (`src/inference/asset_cache.hpp:76`) machinery + tests pass (104/104 ctest), but `grep -rn 'AssetCache\|infer_cached' src/compose src/orchestrator` returns empty — no production effect / runtime call site invokes the cache. M11 exit criterion at `docs/MILESTONES.md:137` requires inference assets to **flow through** the cache, not just have machinery available. **方向：** wire `AssetCache::get` / `put` into the (yet-to-land) compose-graph stages that invoke `me::inference::Runtime::run`. Pattern from `tests/test_inference_content_hash_cache.cpp`'s `infer_cached` helper: compute key from `(model_id, model_version, quantization, hash_inputs(inputs))`; on hit return cached outputs; on miss delegate to `Runtime::run` + store. Add the cache as a member of the engine impl (`src/core/engine_impl.hpp`) so all effect stages share one process-wide cache instance. Milestone §M11，Rubric §5.1。
- **inference-load-model-blob-wire-effect-stages** — `me::inference::load_model_blob` (`src/inference/model_loader.hpp:84`) + tests pass; `grep -rn 'load_model_blob' src/compose src/orchestrator src/api` returns empty — no production model-loading path invokes it, so license whitelist + content-hash gates aren't actually enforced when an effect needs a model. M11 exit criterion at `docs/MILESTONES.md:138` requires the engine to **enforce** the license whitelist, not just have the helper available. **方向：** at first `me::inference::Runtime` instantiation per model, the runtime factory calls `load_model_blob` to fetch + validate; reject runtime construction on non-whitelisted license. Cache the validated `LoadedModel` so subsequent invocations don't re-fetch (`engine->loaded_models` map keyed on model_id+version+quantization). Pair with effect-stage wiring (P0 bullet above) so the path is exercised end-to-end. Milestone §M11，Rubric §5.1。
- **face-sticker-compose-graph-stage-impl** — `src/compose/face_sticker_kernel.cpp` (kernel) + `src/compose/sticker_decoder.cpp` + `src/compose/landmark_resolver.cpp` (resolvers, cycle b8ddb48) all exist; `grep -rn 'face_sticker.*Stage\|face_sticker.*TaskKindId' src/orchestrator src/task` returns empty — no compose-graph stage registers / dispatches the chain at render time, so a timeline JSON with `face_sticker` effect parses but doesn't do anything visible at render. M11 exit criterion at `docs/MILESTONES.md:141-145` requires ≥3 detection-driven effects functioning end-to-end. **方向：** new `src/compose/face_sticker_stage.{hpp,cpp}` that registers a `TaskKindId::FaceStickerStage` (or similar; check `src/task/registry.cpp` for existing registration patterns); the stage's `dispatch` calls sticker_decoder → landmark_resolver → apply_face_sticker_inplace in sequence with TaskContext-injected resources. Wire into `src/orchestrator/compose_compile.cpp`'s effect-dispatch table so timeline `face_sticker` kinds compile to this TaskKind. End-to-end test: timeline JSON with face_sticker effect + landmark fixture + PNG → render produces frames with sticker overlaid (currently `tests/test_face_sticker_compose_stage.cpp` covers the chain but not the orchestrator dispatch). Milestone §M11，Rubric §5.1。

## P1（强烈建议，跨 milestone debt）

- **me-output-spec-typed-codec-enum** — `docs/PAIN_POINTS.md` 2026-04-22 records: `me_output_spec_t.video_codec` / `audio_codec` are `const char*`, every codec addition forces a new `is_xxx_spec` helper + `strcmp` branch; `video_bitrate_bps` shared without per-codec partition. With cycle 11's HEVC landing we now have 3 codecs (passthrough, h264, hevc, hevc-sw) and the typed-option-union decision-point has fired per the bullet's threshold. **方向：** typed option union across the C ABI (`me_video_codec_t` enum + per-codec `me_h264_opts_t`, `me_hevc_opts_t`, `me_aac_opts_t` struct + `me_output_spec_t` with tagged-pointer extensions). Major ABI evolution — needs design discussion before implementation; cycle 25 deferred on §3a.10 ABI-break risk. Milestone §M7-debt (cross)，Rubric §5.2。
- **talevia-jvm-wrapper** — M7 exit criterion "在 talevia 内建 platform-impls/video-media-engine-jvm". Cross-repo task — needs talevia-side work; in this repo `bindings/jni/` is ready (cycle 28, commit 89b7275). **方向：** copy JNI wrapper to talevia tree + build.gradle.kts + replace shell-out FFmpeg in passthrough tests. This repo cycle can only prepare the JNI wrapper; integration lives in talevia. Milestone §M7，Rubric §5.5。
- **inverse-tonemap-hable-impl** — Cycle 24 landed `Linear` in `src/compose/inverse_tonemap_kernel.cpp:apply_inverse_tonemap_inplace`; `Hable` algo still returns ME_E_UNSUPPORTED at line 70 (gated by an explicit early-return). Real Hable inverse needs linear-light float buffers: the curve `f(x) = ((x*(A*x+C*B)+D*E)/(x*(A*x+B)+D*F))-E/F` is invertible only on the [0, white_scale] domain, and byte-domain inverse rounding loses enough precision that the round-trip fails VISION §3.1 byte-identity. **方向：** when a 16-bit (or RgbaF32) working buffer arrives in M11+, replace the Hable UNSUPPORTED branch with the proper linear-light inverse: read SDR byte → linearize via inverse-sRGB-EOTF → numerical inverse of Hable curve via Newton–Raphson on the [0, white_scale] domain → encode to PQ / HLG / linear-light HDR buffer. Until then leave UNSUPPORTED as the deterministic answer for Hable. Milestone §M11-followup，Rubric §5.2。
- **ml-ship-path-model-blazeface** — `grep -rni 'blazeface\|face_landmarks_v2\|selfie_seg' src/ include/` returns empty (only test fixture strings hit). M11 exit criterion 6 (docs/MILESTONES.md:139) requires "≥2 ship-path models running through the runtime + reference parity within ε". BlazeFace + portrait_segmentation are the Apache-licensed candidates per the criterion text. **方向：** add `examples/14_ml_face_landmark/` + `tests/test_inference_blazeface.cpp` that loads BlazeFace through the model-fetcher API + runs inference + asserts CoreML-vs-ONNX-CPU parity within ε. Synthetic fixture path: pre-bake a trivial ONNX model (Constant op or Identity op, ~200 bytes) as a hex C array — no Python build dep, no real BlazeFace bytes shipped; the test exercises the `Runtime::run` contract end-to-end with deterministic output. Real BlazeFace integration via host-supplied blob is the production path tested via env-var-gated path. Milestone §M11，Rubric §5.1。
- **face-mosaic-compose-graph-stage-impl** — `src/compose/face_mosaic_kernel.cpp` exists; `grep -rn 'face_mosaic.*Stage\|face_mosaic.*TaskKindId' src/orchestrator src/task` returns empty — sibling to `face-sticker-compose-graph-stage-impl` (P0). Kernel uses landmark_resolver too (same upstream as face_sticker). **方向：** new `src/compose/face_mosaic_stage.{hpp,cpp}` registering TaskKindId; wire into compose_compile.cpp dispatch. Reuse landmark_resolver for the bbox feed; mosaic kernel applies block-mean / box-blur per bbox (already implemented). End-to-end test mirrors face_sticker — different effect, same chain shape. Milestone §M11，Rubric §5.1。
- **body-alpha-key-mask-resolver-impl** — `src/compose/landmark_resolver.{hpp,cpp}` exists; `grep -rn 'mask_resolver\|MaskResolver\|resolve_mask' src/compose` returns empty. body_alpha_key consumes a mask asset (per-frame alpha sequence), not landmark bboxes — needs a sibling resolver that reads alpha-mask sequences from a fixture file or runtime. **方向：** new `src/compose/mask_resolver.{hpp,cpp}` mirroring landmark_resolver's API: `resolve_mask_alpha_from_file(file_uri, time, *out_width, *out_height, *out_alpha_bytes, *err)`. JSON fixture format: `{"frames": [{"t": {...}, "width": W, "height": H, "alphaB64": "..."}, ...]}` — base64-encoded alpha-mask bytes. Test follows test_face_sticker_compose_stage.cpp shape but with mask + body_alpha_key kernel. Milestone §M11，Rubric §5.1。
- **body-alpha-key-compose-graph-stage-impl** — sibling to face_sticker / face_mosaic stages but uses `mask_resolver` (above bullet) instead of `landmark_resolver`. `grep -rn 'body_alpha_key.*Stage\|body_alpha_key.*TaskKindId' src/orchestrator src/task` returns empty. **方向：** new `src/compose/body_alpha_key_stage.{hpp,cpp}`; depends on `body-alpha-key-mask-resolver-impl` landing first. Same wiring shape as face_sticker stage. Milestone §M11，Rubric §5.1。
- **debt-stub-annotate-unaccounted** — `bash tools/scan-debt.sh` (2026-05-02 snapshot) reports 6 raw `ME_E_UNSUPPORTED` returns lacking either `STUB:` (with backlog slug) or `LEGIT:` (with reason) markers — debt scan rule §R.5 #2. Unaccounted spread: src/orchestrator/reencode_video.cpp:5, src/orchestrator/exporter.cpp:2, src/orchestrator/compose_frame.cpp:2 (totals don't sum to 6 — script's "Unaccounted" is post-LEGIT-deduction). **方向：** for each unaccounted return, add either a `LEGIT: <reason>` comment line (if the rejection is the documented outcome of a closed-set dispatch table) or a `STUB: <slug>` comment + a corresponding bullet in BACKLOG (if the rejection represents deferred impl). Re-run `tools/scan-debt.sh` until "Unaccounted" reports 0. The annotation discipline catches future stub regressions cleanly via `tools/scan-debt.sh` exit code. Milestone §M11-debt，Rubric §5.4。

## P2（未来，当前 milestone 不挤占）

- **examples-14-face-sticker** — M11 exit criterion at `docs/MILESTONES.md:146` requires "每个 effect 有像素级回归测试 + 至少一个 example（候选：`12_face_sticker`、`13_body_alpha_key`）". `ls examples/` shows 10_player_pause_seek / 11_error_diagnosis / 12_cache_invalidate occupy slots 10-12; 14+ are free. **方向：** new `examples/14_face_sticker/` with `main.cpp` + `sample.timeline.json` + a tiny PNG sticker fixture + a JSON landmark fixture. Demonstrates timeline → render → output MP4 with the sticker overlaid. Mirrors examples/01_passthrough's shape: link media_engine, parse timeline JSON, call render_start, write output. Milestone §M11，Rubric §5.1。
- **examples-15-body-alpha-key** — sibling of `examples-14-face-sticker` for body_alpha_key. **方向：** `examples/15_body_alpha_key/` with timeline + mask fixture + tiny PNG. Demonstrates green-screen-without-greenscreen via the body_alpha_key effect. Same shape as #12. Milestone §M11，Rubric §5.1。
- **pixel-regression-test-face-sticker-kernel** — M11 exit criterion at `docs/MILESTONES.md:146` requires "每个 effect 有像素级回归测试". `tests/test_face_sticker_stub.cpp` already covers argument-shape + opaque-fill behavior (14 cases); this bullet calls for a tighter pixel-byte-exact regression that pins the deterministic blend output bytes for a known sticker + landmark + canvas combo. `grep -n 'CHECK.*0x..' tests/test_face_sticker_stub.cpp` — the existing assertions are mostly `CHECK_EQ` on R/G/B/A separately for one or two pixels, not full-frame byte-identical. **方向：** new `tests/test_face_sticker_kernel_pixel.cpp` that pre-computes (via Python or hand) the expected RGBA8 bytes for a 64x64 frame + 16x16 sticker + (16,16,32,32) bbox, then runs apply_face_sticker_inplace and asserts byte-identical output. Same pattern for face_mosaic + body_alpha_key in sibling tests. Milestone §M11，Rubric §5.1。
- **encode-hevc-main10-kvazaar-source-build** — `src/io/kvazaar_hevc_encoder.cpp:88` calls `kvz_api_get(8)` exclusively; the homebrew Kvazaar bottle's `kvazaar.h` defaults to `KVZ_BIT_DEPTH 8` and the prebuilt library doesn't expose a 10-bit `kvz_pixel` ABI. `cfg->input_bitdepth = 10` is accepted by `encoder_open` but the `picture_alloc` buffer geometry stays 8-bit. M10 exit criterion 3 (docs/MILESTONES.md:120) was satisfied by the M10 SW HEVC fallback shipping Main 8-bit only; HDR Main 10 stays on the VideoToolbox HW path. This bullet is the future-improvement to bring SW HEVC up to Main 10 for cross-host HDR. **方向：** replace `pkg_check_modules(kvazaar)` with `FetchContent_Declare(kvazaar URL ...)` + `cmake -DKVZ_BIT_DEPTH=10` so the engine links its own 10-bit-built libkvazaar.a; rewrite the encoder TU's plane-copy loops to pass uint16_t buffers. Add a fixture YUV420P 10-bit gray frame and a round-trip test that decodes the produced HEVC Main 10 and verifies pixel domain matches within ε. Note: Kvazaar's build system is autotools (no native CMake); FetchContent will need ExternalProject_Add wrapping. Milestone §M10-followup，Rubric §5.2。
